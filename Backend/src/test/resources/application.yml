# 테스트 환경 전용 설정 (H2 인메모리 DB 사용)
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop # 테스트 시작 시 스키마를 생성하고, 종료 시 삭제